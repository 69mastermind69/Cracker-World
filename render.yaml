services:
  - type: worker
    name: all-in-one-telegram-bot
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: BOT_TOKEN
        sync: false

      - key: ADMIN_ID
        sync: false
